import Component from '../lib/component';

const NavToggle = new Component( {
    el: '#nav-toggle',
    init() {
        this.bindEvents();

    },

    bindEvents() {
	},

	toggleNav() {
	}
} );

export default NavToggle;
